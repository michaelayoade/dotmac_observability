# Suppression rules.
#
# Measured against the live Alertmanager on 2026-08-30 and originally
# transcribed exactly, including its `equal` list. That transcribed rule was
# removed on 2026-09-13: it was structurally dead. Its `equal = ["alertname",
# "instance"]` condition requires a matching critical/warning pair to share
# the same `alertname`, and no pair of live rules ever does — that alone is
# enough for the rule to never fire, so it suppressed nothing. There is
# currently no live inhibition rule; this file keeps a schema-valid empty
# collection rather than being deleted.

schema_version = "observability-routing.v2"
kind = "inhibition"

rules = []
