# GENERATED by `dotmac-observability render` — do not edit.
# Source of truth: inventory/, bundles/ and routing/ in dotmac_observability.
# `make render-check` compares these bytes against a fresh render and fails on drift.
apiVersion: 1
datasources:
  - name: metrics
    type: prometheus
    access: proxy
    url: "http://prometheus:9090"
    isDefault: true
    editable: false
  - name: logs
    type: loki
    access: proxy
    url: "http://loki:3100"
    isDefault: false
    editable: false
  - name: status-metrics
    type: prometheus
    access: proxy
    url: "https://status.dotmac.invalid:443"
    isDefault: false
    editable: false
    uid: status-metrics
