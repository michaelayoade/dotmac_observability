# GENERATED by `dotmac-observability render` — do not edit.
# Source of truth: inventory/, bundles/ and routing/ in dotmac_observability.
# `make render-check` compares these bytes against a fresh render and fails on drift.
# address family: ipv4
*filter

# ssh: ingress tcp/22 (host_service, ipv4 terminates on INPUT)
-A INPUT -p tcp --dport 22 -m conntrack --ctstate RELATED,ESTABLISHED -j ACCEPT
-A INPUT -i wg0 -p tcp --dport 22 -j ACCEPT
-A INPUT -p tcp --dport 22 -j DROP

# prometheus: loopback tcp/9090 (container_published, ipv4 terminates on DOCKER-USER)
-A DOCKER-USER -p tcp --dport 9090 -j DROP

# alertmanager: loopback tcp/9093 (container_published, ipv4 terminates on DOCKER-USER)
-A DOCKER-USER -p tcp --dport 9093 -j DROP

COMMIT
